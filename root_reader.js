/* This code can read the system/root level files without actually logging as root or using sudo if 'sudo su' or sudo command is blocked...
*/

const fs = require('fs');

fs.readFile('<directory here>', 'utf8', (err, data) => { if (err) throw err; console.log(data); Agent 102-0) Deck/2000 Preter/102.6
ept-Language; en US enged.